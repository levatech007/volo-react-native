import { StyleSheet } from 'react-native';

const locationStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  logo: {
  flex: 1,
  width: 300,
  }
});

export { locationStyles }
